# Этот файл отключён — сборка перенесена в build.yml
# Удалите этот файл или оставьте как есть, он не запускается.

name: mac-build (disabled)

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'Этот воркфлоу отключён. Используйте build.yml'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Используйте .github/workflows/build.yml"
